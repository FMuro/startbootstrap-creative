{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build site",
            "type": "shell",
            "command": "./convert_images.sh && ./bibliographer/bibliographer.py && ./populate.py && npm run build",
        }
    ]
}